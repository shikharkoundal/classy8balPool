// script/system/Global.js
export const BORDER_SIZE = 62;
export const HOLE_RADIUS = 45;
export const BALL_SIZE = 50; // diameter
export const BALL_RADIUS = BALL_SIZE / 2;

// default game canvas size (can be overridden by Game.start)
export const DEFAULT_GAME_WIDTH = 1500;
export const DEFAULT_GAME_HEIGHT = 825;

// misc flags (modules should prefer Game instance flags when possible)
export const DEFAULT_TRAIN_ITER = 30;
